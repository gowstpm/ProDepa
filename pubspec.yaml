name: flutter_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.1
  firebase_core: ^3.6.0
  google_fonts: ^6.2.1
  # Add other dependencies here if needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Include any other dev dependencies you need

# Optional dependency versions to update
dependency_overrides:
  collection: ^1.19.0
  http_parser: ^4.1.0
  leak_tracker: ^10.0.7
  leak_tracker_flutter_testing: ^3.0.8
  material_color_utilities: ^0.12.0
  meta: ^1.16.0
  stack_trace: ^1.12.0
  string_scanner: ^1.3.0
  test_api: ^0.7.3
  vm_service: ^14.3.0